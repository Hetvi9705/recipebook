---
layout: base.njk
title: Home - My CookBook
---

<div class="container">
    <!-- HERO SECTION -->
    <section class="hero fade-in">
        <div class="hero-text">
            <h2>Welcome to My CookBook</h2>
            <p>Exploring ideas, Good Food, Good Mood. Discover amazing cooking hacks and inspiring recipe stories.</p>
            <a href="#cooking-hacks" class="hero-btn">Explore Content</a>
        </div>
    </section>

    <!-- QUICK NAVIGATION -->
    <section class="category-nav">
        <a href="#cooking-hacks" class="category-btn">
            <i class="fas fa-lightbulb"></i>
            Cooking Hacks
        </a>
        <a href="#recipe-stories" class="category-btn">
            <i class="fas fa-book-open"></i>
            Recipe Stories
        </a>
        <a href="#featured-posts" class="category-btn">
            <i class="fas fa-star"></i>
            Featured Posts
        </a>
    </section>

    <!-- COOKING HACKS SECTION -->
    <!-- COOKING HACKS SECTION -->
<section id="cooking-hacks" class="content-section cooking-hacks-section">
    <div class="section-header">
        <h2><i class="fas fa-lightbulb"></i> Cooking Hacks</h2>
        <p>Smart tricks and tips to make your cooking easier and more enjoyable</p>
    </div>
    <div class="post-list">
        {% for post in posts %}
            {% if post.category == 'cooking-hack' or 'hack' in post.title or 'tip' in post.title %}
            <article class="post-item fade-in">
                {% if post.featuredImage %}
                <div class="post-image" style="background-image: url('https:{{ post.featuredImage }}'); background-size: cover; background-position: center;"></div>
                {% else %}
                <div class="post-image" style="background: var(--primary-gradient);"></div>
                {% endif %}
                <div class="post-item-content">
                    <h4><a href="/posts/{{ post.slug }}/">{{ post.title }}</a></h4>
                    <div class="post-meta">
                        <span><i class="far fa-calendar"></i> {{ post.publishedDate | readableDate }}</span>
                        {% if post.readTime %}
                        <span><i class="fas fa-clock"></i> {{ post.readTime }} min read</span>
                        {% endif %}
                    </div>
                    <p>{{ post.excerpt }}</p>
                    <a href="/posts/{{ post.slug }}/" class="read-more">
                        Read More <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
            </article>
            {% endif %}
        {% endfor %}
        
        <!-- Fallback if no cooking hack posts found -->
        {% set hasCookingHacks = false %}
        {% for post in posts %}
            {% if post.category == 'cooking-hack' or 'hack' in post.title or 'tip' in post.title %}
                {% set hasCookingHacks = true %}
            {% endif %}
        {% endfor %}
        
        {% if not hasCookingHacks %}
            <!-- Show some placeholder content -->
            <article class="post-item fade-in">
                <div class="post-image" style="background: var(--primary-gradient);"></div>
                <div class="post-item-content">
                    <h4><a href="#">Cooking Hack Posts Coming Soon</a></h4>
                    <div class="post-meta">
                        <span><i class="far fa-calendar"></i> Soon</span>
                        <span><i class="fas fa-clock"></i> 2 min read</span>
                    </div>
                    <p>We're preparing some amazing cooking hacks for you!</p>
                    <a href="#" class="read-more">
                        Coming Soon <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
            </article>
        {% endif %}
    </div>
</section>

    <!-- RECIPE STORIES SECTION -->
    <section id="recipe-stories" class="content-section recipe-stories-section">
        <div class="section-header">
            <h2><i class="fas fa-book-open"></i> Recipe Stories</h2>
            <p>Heartwarming stories behind beloved recipes and family traditions</p>
        </div>
        <div class="post-list">
            
            <!-- Recipe Story 1 -->
            <article class="post-item fade-in">
                <div class="post-image" style="background: linear-gradient(135deg, #ec4899 0%, #db2777 100%);"></div>
                <div class="post-item-content">
                    <h4><a href="/posts/grandmas-sunday-gravy/">Grandma's Sunday Gravy</a></h4>
                    <div class="post-meta">
                        <span><i class="far fa-calendar"></i> November 15, 2024</span>
                        <span><i class="fas fa-clock"></i> 4 min read</span>
                    </div>
                    <p>The story of how a simple pot of tomato sauce became the anchor of our family for generations.</p>
                    <a href="/posts/grandmas-sunday-gravy/" class="read-more">
                        Read More <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
            </article>
            
            <!-- Recipe Story 2 -->
            <article class="post-item fade-in">
                <div class="post-image" style="background: linear-gradient(135deg, #06b6d4 0%, #0891b2 100%);"></div>
                <div class="post-item-content">
                    <h4><a href="/posts/kerala-curry-story/">The Curry That Changed Everything</a></h4>
                    <div class="post-meta">
                        <span><i class="far fa-calendar"></i> November 14, 2024</span>
                        <span><i class="fas fa-clock"></i> 5 min read</span>
                    </div>
                    <p>Lost on a rainy night in Kerala, a stranger's kindness offered more than just shelter from the storm.</p>
                    <a href="/posts/kerala-curry-story/" class="read-more">
                        Read More <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
            </article>
            
            <!-- Recipe Story 3 -->
            <article class="post-item fade-in">
                <div class="post-image" style="background: linear-gradient(135deg, #84cc16 0%, #65a30d 100%);"></div>
                <div class="post-item-content">
                    <h4><a href="/posts/winter-solstice-pie/">A Pie for the Solstice</a></h4>
                    <div class="post-meta">
                        <span><i class="far fa-calendar"></i> November 13, 2024</span>
                        <span><i class="fas fa-clock"></i> 3 min read</span>
                    </div>
                    <p>How baking a spiced pear and cranberry pie became my personal ritual for welcoming the return of light.</p>
                    <a href="/posts/winter-solstice-pie/" class="read-more">
                        Read More <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
            </article>

        </div>
    </section>

    <!-- YOUR EXISTING FEATURED POSTS SECTION -->
    {% set featuredPosts = [] %}
    {% for post in posts %}
        {% if post.featured and featuredPosts.length < 3 %}
            {% set featuredPosts = (featuredPosts.push(post), featuredPosts) %}
        {% endif %}
    {% endfor %}
    {% if featuredPosts.length > 0 %}
    <section id="featured-posts" class="featured-posts content-section">
        <div class="section-header">
            <h2><i class="fas fa-star"></i> Featured Posts</h2>
            <p>Handpicked content from our collection</p>
        </div>
        <div class="post-grid">
            {% for post in featuredPosts %}
            <article class="post-card featured fade-in">
                {% if post.featuredImage %}
                <img src="https:{{ post.featuredImage }}" alt="{{ post.title }}" class="post-image">
                {% endif %}
                <div class="post-content">
                    <h4><a href="/posts/{{ post.slug }}/">{{ post.title }}</a></h4>
                    <p class="post-meta">{{ post.publishedDate | readableDate }} • {{ post.author }}</p>
                    <p>{{ post.excerpt }}</p>
                    <a href="/posts/{{ post.slug }}/" class="read-more">Read More →</a>
                </div>
            </article>
            {% endfor %}
        </div>
    </section>
    {% endif %}

    <!-- YOUR EXISTING RECENT POSTS SECTION -->
    <section id="recent-posts" class="recent-posts content-section">
        <div class="section-header">
            <h2><i class="fas fa-clock"></i> Recent Posts</h2>
            <p>Latest updates from the blog</p>
        </div>
        <div class="post-list">
            {% for post in posts | limit(5) %}
            <article class="post-item fade-in">
                <div class="post-item-content">
                    <h4><a href="/posts/{{ post.slug }}/">{{ post.title }}</a></h4>
                    <p class="post-meta">{{ post.publishedDate | readableDate }} • {{ post.author }}</p>
                    <p>{{ post.excerpt }}</p>
                    <a href="/posts/{{ post.slug }}/" class="read-more">Read More →</a>
                </div>
            </article>
            {% endfor %}
        </div>
    </section>

    <!-- FEATURES SECTION -->
    <section class="features">
        <div class="section-title">
            <h3>Why Choose CookBook</h3>
        </div>
        <div class="features-grid">
            <div class="feature-card fade-in">
                <div class="feature-icon">
                    <i class="fas fa-lightbulb"></i>
                </div>
                <h3>Smart Hacks</h3>
                <p>Discover clever cooking tricks that save time and elevate your meals.</p>
            </div>
            <div class="feature-card fade-in">
                <div class="feature-icon">
                    <i class="fas fa-book-open"></i>
                </div>
                <h3>Inspiring Stories</h3>
                <p>Read heartfelt stories behind beloved recipes and family traditions.</p>
            </div>
            <div class="feature-card fade-in">
                <div class="feature-icon">
                    <i class="fas fa-heart"></i>
                </div>
                <h3>From the Heart</h3>
                <p>Every recipe and story comes from real experiences and traditions.</p>
            </div>
            <div class="feature-card fade-in">
                <div class="feature-icon">
                    <i class="fas fa-users"></i>
                </div>
                <h3>Community</h3>
                <p>Join food lovers sharing their culinary journeys and discoveries.</p>
            </div>
        </div>
    </section>
</div>

<style>
    /* Additional styles for the new sections */
    .category-nav {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin: 40px 0;
        flex-wrap: wrap;
    }

    .category-btn {
        background: white;
        border: 2px solid var(--border-color);
        padding: 15px 30px;
        border-radius: var(--radius);
        font-weight: 600;
        text-decoration: none;
        color: var(--text-color);
        transition: var(--transition);
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .category-btn:hover {
        border-color: var(--primary-color);
        transform: translateY(-3px);
        box-shadow: var(--shadow-md);
    }

    .content-section {
        margin: 60px 0;
    }

    .section-header {
        text-align: center;
        margin-bottom: 40px;
    }

    .section-header h2 {
        font-size: 2.5rem;
        color: var(--text-color);
        display: inline-block;
        position: relative;
    }

    .section-header h2::after {
        content: '';
        display: block;
        width: 80px;
        height: 4px;
        background: var(--primary-gradient);
        margin: 15px auto;
        border-radius: 2px;
    }

    .cooking-hacks-section {
        border-left: 4px solid var(--primary-color);
        padding-left: 20px;
    }

    .recipe-stories-section {
        border-left: 4px solid var(--accent-color);
        padding-left: 20px;
    }

    /* Hero section styles */
    .hero {
        background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://images.unsplash.com/photo-1504674900247-0877df9cc836?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80') center/cover no-repeat;
        color: #ffffff;
        text-align: center;
        padding: 120px 20px;
        border-radius: var(--radius-lg);
        margin: 30px auto;
        max-width: 1200px;
        position: relative;
        overflow: hidden;
    }

    .hero h2 {
        font-size: 3.5rem;
        margin-bottom: 20px;
        font-weight: 800;
        line-height: 1.2;
    }

    .hero p {
        font-size: 1.25rem;
        margin-bottom: 30px;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
        opacity: 0.9;
    }

    .hero-btn {
        display: inline-block;
        background: var(--primary-gradient);
        color: #fff;
        padding: 14px 32px;
        border-radius: 50px;
        font-weight: 600;
        text-decoration: none;
        transition: var(--transition);
        box-shadow: 0 4px 15px rgba(37, 99, 235, 0.3);
    }

    .hero-btn:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 25px rgba(37, 99, 235, 0.4);
    }

    /* Ensure your existing post styles work */
    .post-list {
        display: grid;
        gap: 30px;
    }

    .post-item {
        background: var(--bg-color);
        border-radius: var(--radius-lg);
        overflow: hidden;
        box-shadow: var(--shadow-md);
        transition: var(--transition);
    }

    .post-item:hover {
        transform: translateY(-5px);
        box-shadow: var(--shadow-lg);
    }

    .post-image {
        height: 200px;
        background: var(--primary-gradient);
        position: relative;
        overflow: hidden;
    }

    .post-item-content {
        padding: 25px;
    }

    .read-more {
        color: var(--primary-color);
        text-decoration: none;
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        transition: var(--transition);
    }

    .read-more:hover {
        gap: 12px;
    }
</style>

<script>
    // Smooth scrolling for navigation
    document.addEventListener('DOMContentLoaded', function() {
        document.querySelectorAll('nav a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Also make category buttons scroll smoothly
        document.querySelectorAll('.category-btn[href^="#"]').forEach(button => {
            button.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    });
</script>